/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: Cross-origin dev warning from adf.nyanfox.com can be safely ignored
  // Future Next.js versions may require explicit allowedDevOrigins configuration
}

module.exports = nextConfig
